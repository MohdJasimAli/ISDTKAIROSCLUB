{
  "name": "isdt-kairos-club",
  "version": "0.1.0",
  "private": true,
  "description": "ISDT Kairos Club monorepo root. backend/ and frontend/ deploy as separate services (Render API + Vercel web); these root scripts let Render's default root-level Node build/start reach the backend with zero dashboard edits.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install --include=dev && npm --prefix backend run db:generate",
    "start": "npm --prefix backend start"
  }
}
